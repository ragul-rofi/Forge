{
    "name": "forge",
    "version": "1.0.0",
    "description": "Student Career Profiler - Don't find your path. Forge it.",
    "private": true,
    "workspaces": [
        "client",
        "admin",
        "server"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev:client\" \"npm run dev:admin\" \"npm run dev:server\"",
        "dev:client": "cd client && npm run dev",
        "dev:admin": "cd admin && npm run dev",
        "dev:server": "cd server && npm run dev",
        "build": "cd client && npm run build",
        "build:admin": "cd admin && npm run build",
        "build:all": "concurrently \"npm run build\" \"npm run build:admin\"",
        "install:all": "npm install && cd client && npm install && cd ../admin && npm install && cd ../server && npm install"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}